battery_entity:
  card:
    type: custom:auto-entities
    show_empty: false
    card:
      type: entities
      title: [[title]]
    filter:
      include:
        - name: "*[[filter]]*"
          attributes:
            device_class: battery