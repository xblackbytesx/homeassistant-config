- alias: Attic | Motion | Lights off
  initial_state: True
  trigger:
    platform: state
    entity_id: binary_sensor.motion_sensor_attic
    to: 'off'
    for:
      minutes: 5
  action:
    - service: homeassistant.turn_off
      entity_id: light.attic
